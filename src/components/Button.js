import React, { useState } from 'react';

export const songButton = () => {
}

/* Logic:

1. const handleClick = () => {
  return (
        <ul>
            {
                articles.map(art =>  (
                    <li key={art.id}>
                        {art.snippet}
                    </li>
                ))
            }
        </ul>
    )
}  
 
2. const handleClick = () => {
  return (
    <ul>
      {Object.values(artists[e.target.id]).forEach(value => <li>{value}</li>)}
    </ul>
  )
}

*/
